# pnpm 11 project settings for the standalone server project.
# allowBuilds is the allowlist of packages permitted to run install scripts —
# pnpm blocks all others by default (supply-chain hardening). esbuild is
# required by tsx (dev/mcp runtime) and must build its native binary.
allowBuilds:
  esbuild: true
