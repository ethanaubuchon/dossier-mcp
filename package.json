{
  "name": "dossier-mcp",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "pnpm -C server dev",
    "mcp": "pnpm -C server mcp",
    "test": "pnpm -C server test",
    "build": "pnpm -C server build",
    "install:all": "pnpm -C server install"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
