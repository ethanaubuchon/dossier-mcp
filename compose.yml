services:
  server:
    build:
      context: ./server
      target: dev
    ports:
      - "3001:3001"
    volumes:
      - ./server/src:/app/src
      - ./notes:/notes
    environment:
      - NODE_ENV=development
      - NOTES_DIR=/notes
    restart: unless-stopped
