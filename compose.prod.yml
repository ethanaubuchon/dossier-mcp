services:
  server:
    build:
      context: ./server
      target: prod
    ports:
      - "3001:3001"
    volumes:
      - notes_data:/notes
    environment:
      - NODE_ENV=production
      - NOTES_DIR=/notes
    restart: unless-stopped

volumes:
  notes_data:
